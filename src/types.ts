export enum Fruits {
  Apple,
  Orange,
  Pear,
}

export enum Phase {
  Start,
  Playing,
  GameOver,
  Victory,
}

export enum Die {
  Pick1,
  Pick2,
  Pick3,
  CrowForward,
  CrowBack,
  PickAny,
}

export enum Backend {
  Zustand,
  Valtio,
  Jotai,
}
